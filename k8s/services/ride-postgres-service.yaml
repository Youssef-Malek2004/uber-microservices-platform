apiVersion: v1
kind: Service
metadata:
  name: ride-postgres
  namespace: uber
  labels:
    app: ride-postgres
spec:
  clusterIP: None
  selector:
    app: ride-postgres
  ports:
    - port: 5432
      targetPort: 5432