apiVersion: v1
kind: Service
metadata:
  name: driver-service
  namespace: uber
  labels:
    app: driver-service
spec:
  type: ClusterIP
  selector:
    app: driver-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
